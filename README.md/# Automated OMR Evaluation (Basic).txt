# Automated OMR Evaluation (Basic)

## 📌 About
A very simple OMR evaluation system using **Python + OpenCV + Streamlit**.  
Uploads an OMR image, compares with a dummy answer key, and gives score.

## 🚀 Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
